import { styled } from "./stitches.config";

const SubmitBtn = styled('button', {
    padding: '0.5rem 1rem',
    borderRadius: '15px',
    backgroundColor: '#5BDEC3',
    color: '$colorWhite'
});

export {
    SubmitBtn
};